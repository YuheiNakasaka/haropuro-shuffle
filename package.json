{
  "name": "haropuro-shuffle",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "start": "wrangler pages dev --kv HAROPURO_SHUFFLE -- npx vite"
  },
  "dependencies": {
    "preact": "^10.5.13"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.2.0",
    "@preact/preset-vite": "^2.0.0",
    "typescript": "^4.3.2",
    "vite": "^2.6.4"
  }
}
